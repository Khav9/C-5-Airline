export class Meal {
    constructor(private name: string, private position: number, private salary: number) {
        this.name = name;
        this.position = position;
        this.salary = salary;
    }
}